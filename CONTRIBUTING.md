# Panustamine

Aitäh, et soovid panustada! 🎉 Projekt on avatud [CC BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/) litsentsi all.

## Kuidas aidata?

1. **Forki** see repo
2. Loo uus **haru** (`git checkout -b fix/kirjeldus`)
3. Tee muudatused `docs/` kaustas
4. **Commit** ja **push**
5. Ava **Pull Request**

## Mida ootame?

- Kirjavigade ja katkiste linkide parandusi
- Tehnilisi täpsustusi (vale käsk, aegunud info)
- Paremaid selgitusi ja näiteid

## Reeglid

- Muuda ainult `docs/` kaustas olevaid faile
- Ära muuda `site/`, `mkdocs.yml` struktuuri ega `overrides/` kausta
- Pildid `docs/assets/` kausta, ainult `.png` formaadis, lowercase nimed
- Tabelite captionid tabeli **all**, italic formaadis (`*Tabel X.Y. Kirjeldus*`)
- Iga pilt `<figure>` + `<figcaption>` sees
- Footnote viited kirjeldavad (`[^rfc791]`), mitte numbrilised (`[^1]`)
- Koodiplokid alati keele annotatsiooniga (` ```bash `, ` ```text ` jne)

## Küsimused?

Ava [Issue](https://github.com/optalvik/ssl/issues).
